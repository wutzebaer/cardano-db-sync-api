# update-dependencies

Prüfe auf dependency und plugin updates mit den befehlen

  - ./mvnw versions:display-dependency-updates | grep -- '->'
  - ./mvnw versions:display-plugin-updates | grep -- '->'

Dann update die versionen.